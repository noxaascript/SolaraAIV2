# core/api README

This directory contains HTTP API handlers and routing logic.
It is purposely kept as a plain directory (no __init__.py) so it can
act as a namespace for many implementation files.

Remove any __init__.py in this folder if present to make it a namespace package.
