# core/application README

Application orchestration modules live here (startup, lifecycle, CLI runners).
Add modules that implement runner, server bootstrap, and lifecycle hooks.

Remove any __init__.py in this folder if present to make it a namespace package.
