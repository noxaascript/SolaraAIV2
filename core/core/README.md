# core/core README

Shared core utilities, helpers and lightweight modules. This directory
is a plain directory and not a Python package by default.

Remove any __init__.py in this folder if present to make it a namespace package.
